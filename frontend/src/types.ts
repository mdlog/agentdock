export type Metrics = { success_rate: number; uptime_pct: number; latency_sec: number; task_volume: number; recency_score: number; feedback_score: number; reputation_score: number };
export type Agent = { id: string; name: string; tagline: string; description: string; category: string; capabilities: string[]; price_usd: number; status: "active" | "offline"; metrics: Metrics; identity: { chain_id: number; registry: string; agent_id: number; metadata_verified: boolean; endpoint_verified: boolean; source: string }; output_schema: string[]; updated_at: string };
export type OnchainAgent = { id: string; chain_id: number; is_testnet: boolean; token_id: number; name: string; description: string; image_url?: string; has_source_icon: boolean; owner_address?: string; contract_address?: string; supported_protocols: string[]; x402_supported: boolean; is_verified: boolean; total_score?: number; rank?: number; health_score?: number; total_feedbacks: number; average_score?: number; source_label: string; synced_at: string };
export type Readiness = { chain_id: number; registry_configured: boolean; rpc_reachable: boolean; registry_has_code: boolean; b402_ready: boolean; agent_endpoints_ready: boolean; object_storage_ready: boolean; storage_mode: string; notes: string[] };
export type Task = { id: string; agent_id: string; agent_name: string; objective: string; constraints: string; wallet_address?: string; state: string; estimated_price_usd: number; quote_id?: string; quote_expires_at?: string; tx_hash?: string; result?: Record<string, unknown>; created_at: string; updated_at: string };
export type AuditEvent = { id: string; task_id: string; event: string; detail: string; created_at: string };